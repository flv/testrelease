sudo: required

language: bash
script:
  - mkdir bundles
  - touch bundles/things
  - tar czvf bundles/bidule.tgz README.md

services:
  - docker

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y vim 
